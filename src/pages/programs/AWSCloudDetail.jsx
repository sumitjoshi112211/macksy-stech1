import ProgramDetails from "../../components/ProgramDetails";
import { programDetailsData } from "../../data/programDetailsData";

const AWSCloudDetail = () => {
  return <ProgramDetails programData={programDetailsData.awscloud} />;
};

export default AWSCloudDetail;
